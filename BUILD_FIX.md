# Zeabur 建置失敗 - 全面解決方案

## 🔍 可能的原因分析

### 1. PostCSS 配置問題
- **問題**：`postcss.config.mjs` 在 Zeabur 環境可能無法正確載入
- **影響**：CSS 處理失敗，導致建置中斷

### 2. Webpack 路徑別名問題
- **問題**：`@/` 路徑別名在生產環境解析失敗
- **影響**：模組找不到，導致建置失敗

### 3. 依賴套件問題
- **問題**：某些套件在生產環境安裝或載入失敗
- **影響**：建置過程出錯

### 4. Node.js 版本不匹配
- **問題**：Zeabur 使用的 Node.js 版本與本地不同
- **影響**：某些語法或 API 不支援

### 5. 環境變數在建置時被檢查
- **問題**：程式碼在建置時就檢查環境變數
- **影響**：建置階段沒有環境變數導致失敗

## 🛠️ 全面解決方案

### 方案 1: 修正 PostCSS 配置（最可能的原因）

將 `postcss.config.mjs` 改回 CommonJS 格式，但使用更明確的寫法：

```javascript
// postcss.config.js
module.exports = {
  plugins: [
    require('tailwindcss'),
    require('autoprefixer'),
  ],
};
```

### 方案 2: 簡化 next.config.js

移除可能有問題的 webpack 配置，使用更簡單的方式：

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  // 移除 experimental 和 webpack 配置，使用預設值
}

module.exports = nextConfig
```

### 方案 3: 確保 package.json 正確

確認所有依賴都在 `dependencies` 而不是 `devDependencies`（生產環境需要）：

```json
{
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "openai": "^4.47.0",
    "sharp": "^0.33.2",
    "zod": "^3.22.4",
    "tailwindcss": "^3.4.1",
    "postcss": "^8.4.35",
    "autoprefixer": "^10.4.17"
  }
}
```

### 方案 4: 建立 .nvmrc 指定 Node 版本

```bash
# .nvmrc
18.20.0
```

### 方案 5: 建立 zeabur.json 配置

```json
{
  "buildCommand": "npm install && npm run build",
  "startCommand": "npm run start",
  "installCommand": "npm install --legacy-peer-deps"
}
```

## 🎯 推薦的修正步驟

1. **先修正 PostCSS 配置**（最常見問題）
2. **簡化 next.config.js**
3. **移動必要依賴到 dependencies**
4. **測試本地建置**

## 📝 檢查清單

- [ ] PostCSS 配置格式正確
- [ ] next.config.js 沒有複雜的 webpack 配置
- [ ] 所有生產環境需要的套件都在 dependencies
- [ ] 本地可以成功建置
- [ ] 沒有在建置時檢查環境變數
